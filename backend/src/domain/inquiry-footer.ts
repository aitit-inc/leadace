// Phrasing for the inquiry-landing line. Shared by the email footer and
// the form / SNS path so the wording stays identical across channels.
export function inquiryFooterLine(inquiryUrl: string): string {
  return `Learn more or ask anything: ${inquiryUrl}`
}
