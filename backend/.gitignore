node_modules/
.wrangler/
dist/
sim/out/
# Globbed so editor/CLI backup copies (.dev.vars.bak-*, .dev.vars.old) are
# ignored too: the public mirror shares this working tree layout.
.dev.vars*
!.dev.vars.example
!.dev.vars.mcp.example
*.env
.env.*
!.env.example
